output_dir: results/research_platform/datasets/path_aware_reliability_innovation_l191
actor_run_dir: results/research_platform/rl/path_conditioned_l185_seed20261901_60k_v2
actor_checkpoint: results/research_platform/rl/path_conditioned_l185_seed20261901_60k_v2/checkpoints/step_000050000.pt
seed: 20260723

action_noise_std: 0.03
random_action_probability: 0.05
require_disjoint_environments: true

# Fresh episode seeds are used after the L190 diagnostic.  Geometry roles
# remain disjoint and L186/seeds 561--565 remain absent.
splits:
  validation:
    source: training
    roles: [seen]
    scene_prefixes:
      - high_dynamic_accel_straight_l56
      - high_dynamic_accel_turn_l56
    episodes_per_environment: 2
    max_steps: 180
    seed_base: 22842000
  test:
    source: training
    roles: [seen]
    scene_prefixes:
      - high_dynamic_sweep_l56
      - high_dynamic_chicane_l56
    episodes_per_environment: 2
    max_steps: 180
    seed_base: 22843000
  unseen:
    source: validation
    roles: [seen, unseen]
    scene_prefixes:
      - high_dynamic_reverse_s_unseen_l56
      - high_dynamic_hairpin_unseen_l56
    episodes_per_environment: 2
    max_steps: 180
    seed_base: 22844000
