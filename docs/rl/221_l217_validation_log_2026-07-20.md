# L217 交付验证记录

## 数据审计

- 420/420 episodes；
- 420 unique cells；
- 60/60 randomized complete blocks；
- 0 duplicate；
- 0 qualification rows；
- 420/420 resolved config、trajectory、metrics、provenance 完整；
- 420/420 canonical config hash 一致；
- 9/9 checkpoint/calibration hash 一致；
- 三 shard 的 Git SHA 与 manifest SHA-256 一致。

## 静态编译

```bash
python -m py_compile \
  experiments/rl/merge_sealed_benchmark_shards.py \
  experiments/rl/analyze_final_paper_benchmark.py \
  experiments/rl/summarize_l217_complex_navigation.py \
  experiments/rl/plot_l217_complex_navigation.py
```

结果：通过。

## 回归测试

第一次直接调用 `.venv/bin/pytest` 时，收集阶段因仓库根目录未进入 `PYTHONPATH` 而出现 `ModuleNotFoundError: experiments.rl/src`；没有执行任何 test body。该环境问题被保留并修正为仓库双包布局所需的 `PYTHONPATH=.:src`。

最终命令：

```bash
PYTHONPATH=.:src .venv/bin/pytest -q \
  tests/experiments/test_final_paper_benchmark.py \
  tests/planners/test_paper_rl_driven_mppi.py \
  tests/planners/test_rl_driven_mppi.py \
  tests/planners/test_mppi_dynamics_regression.py \
  tests/learning/test_residual_reliability_gate.py \
  tests/platform/test_mppi_residual_reliability.py \
  tests/rl/test_conservative_terminal_reliability.py \
  tests/rl/test_cross_layer_reliability.py \
  tests/rl/test_gate3_reliability_calibration.py \
  tests/rl/test_multidomain_reliability_gate.py \
  tests/rl/test_residual_reliability_calibration.py \
  tests/rl/test_sealed_reliability_confirmation.py
```

结果：`90 passed in 2.31s`。

## 图形 QA

四组图均生成 PDF 与 300 dpi PNG，并逐张目视检查。轨迹图使用预先可解释的第一个 sealed seed 78006 和 `combined_unseen`，不是按结果选择的 seed；坐标范围包含实际绕行轨迹，没有裁掉地图下方或右侧绕行部分。
