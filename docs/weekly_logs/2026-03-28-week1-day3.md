# 2026-03-28（科研第 3 天）：固定场景实验脚本重构与场景成型

## 今日主线
今天的核心工作，不再是重新理解 MPPI baseline，而是把已经跑通的 `mppi_receding_horizon_demo.py` 从“单场景 demo”推进成“可切换固定场景、可保存结果、可支持跨场景参数比较”的实验脚本。

---

## 今天完成了什么

### 1. 把场景从主函数里抽离出来
将原本写死在 `main()` 里的场景配置，整理到 `experiments/configs/baseline_scenes.py` 中，用统一的 `SCENES` 配置管理不同实验场景。

目前每个场景都会提供：
- `start_state`
- `goal`
- `obstacles`
- `bounds`

这样后续实验时，只需要切换 `scene_name`，就可以在不同固定场景下复用同一套 MPPI 主循环。

---

### 2. 修复了配置与导入相关问题
为了稳定使用：

```python
import experiments.configs.baseline_scenes as baseline_scenes