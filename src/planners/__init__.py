"""Planning helpers and research-only dynamics adapters.

The source package marker also prevents stale legacy bytecode from shadowing
the Python 3 modules in reproducible experiment environments.
"""
